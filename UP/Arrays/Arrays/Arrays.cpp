#include <iostream>
using namespace std;
int main()
{
    //Task1
    /*int grades_count = 0;
    cin >> grades_count;
    int const max_array = 50;
    float grades[max_array];
    double sum_of_grades = 0;
    for (size_t i = 0; i < grades_count; i++)
    {
        cin >> grades[i];
        sum_of_grades += grades[i];
    }
    cout << sum_of_grades / grades_count * 1.0;*/
    
    //Task2
    //int const upper_limit = 24;
    //double count_of_negatives = 0;
    //double sum_of_positive = 0;
    //float grades[upper_limit];
    //for (size_t i = 0; i < upper_limit; i++)
    //{
    //    cin >> grades[i];
    //    if (grades[i] >= 0)
    //    {
    //        sum_of_positive += grades[i];
    //    }
    //    else count_of_negatives +=1;
    //}
    //cout << "Sum of positives= " << sum_of_positive;
    //cout << "Count of negatives= " << count_of_negatives;
    
    //Task 3

    //int const limit = 50;
    //double array_a[limit];
    //double array_b[limit];
    //double sum = 0;
    //for (size_t i = 0; i < limit; i++)
    //{
    //    cin >> array_a[i];
    //}
    //for (size_t i = 0; i < limit; i++)
    //{
    //    cin >> array_b[i];
    //}
    //for (size_t i = 0; i < limit; i++)
    //{
    //    sum+= array_a[i]*array_b[i];
    //}
    //cout << sum;

    //Task 4
   /* int const limit = 50;
    double array_a[limit];
    int min = INT32_MAX;
    int max = 0;
    for (size_t i = 0; i < limit; i++)
    {
        cin >> array_a[i];
        if (array_a[i]<=min)
        {
            min = array_a[i];
        } if (array_a[i] >= max)
        {
            max = array_a[i];
        }
    }
    cout << min << "<--min max-->" << max;*/
    
    //Task 5
    /*int const limit = 5;
    double array_a[limit];
    int max = 0;
    int index = 0;
    for (size_t i = 0; i < limit; i++)
    {
        cin >> array_a[i];
        if (array_a[i] >= max)
        {
            max = array_a[i];
            index = i;
        }
    }
    cout << index << "<--index max-->" << max;*/

    //Task 6
    
}
